# This file is maintained by Walter CI, and may be rewritten.
# https://github.com/piotr-yuxuan/walter-ci
#
# You are free to remove this project from Walter CI realm by opening
# a PR. You may also create another workflow besides this one.

name: Deploy Walter
'on':
  repository_dispatch: null
  workflow_dispatch:
    inputs:
      walter-version:
        description: Walter version
        required: false
        type: string
  push:
    branches:
    - main
    paths:
    - .github/workflows/deploy.yml
concurrency:
  group: deploy-jobs
  cancel-in-progress: false
env:
  GIT_ASKPASS: ${HOME}/.walter-ci/bin/askpass.sh
  GIT_AUTHOR_EMAIL: ${{ secrets.WALTER_GIT_EMAIL }}
  GIT_AUTHOR_NAME: ${{ secrets.WALTER_AUTHOR_NAME }}
  GIT_COMMITTER_EMAIL: ${{ secrets.WALTER_GIT_EMAIL }}
  GIT_COMMITTER_NAME: ${{ secrets.WALTER_AUTHOR_NAME }}
  GIT_PASSWORD: ${{ secrets.GITHUB_TOKEN }}
  WALTER_ACTOR: ${{ secrets.WALTER_ACTOR }}
  WALTER_AUTHOR_NAME: ${{ secrets.WALTER_AUTHOR_NAME }}
  WALTER_GITHUB_PASSWORD: ${{ secrets.WALTER_GITHUB_PASSWORD }}
  WALTER_GIT_EMAIL: ${{ secrets.WALTER_GIT_EMAIL }}
  WALTER_VERSION: ${{ github.event.inputs.walter-version }}
jobs:
  piotr-yuxuan-algo-calisthenics:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/algo-calisthenics
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/algo-calisthenics
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-closeable-map:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/closeable-map
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/closeable-map
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-dove:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/dove
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/dove
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-ephemeral-server:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/ephemeral-server
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/ephemeral-server
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-malli-cli:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/malli-cli
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/malli-cli
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-nabab:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/nabab
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/nabab
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-polygam:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/polygam
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/polygam
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-poor-mans-distributed-cache:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/poor-mans-distributed-cache
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/poor-mans-distributed-cache
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-security-mining:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/security-mining
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/security-mining
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-slava:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/slava
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/slava
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-slava-record:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/slava-record
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/slava-record
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-tsv-processing:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/tsv-processing
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/tsv-processing
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-walter-ci:
    runs-on: ubuntu-latest
    environment:
      name: self
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/walter-ci
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/walter-ci
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
  piotr-yuxuan-welcome-base-api:
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
    - uses: piotr-yuxuan/walter-ci@main
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/welcome-base-api
      run: |-
        walter forward-secret \
          --secret-name "WALTER_ACTOR" \
          --secret-name "WALTER_AUTHOR_NAME" \
          --secret-name "WALTER_GITHUB_PASSWORD" \
          --secret-name "WALTER_GIT_EMAIL"
    - env:
        GITHUB_REPOSITORY: piotr-yuxuan/welcome-base-api
      run: |-
        walter install-workflow \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-ci.edn" --target-yml "walter-ci.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-cd.edn" --target-yml "walter-cd.yml" \
          --source-edn "$HOME/.walter-ci/edn-sources/walter-perf.edn" --target-yml "walter-perf.yml"
